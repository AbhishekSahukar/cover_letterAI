# CoverLetter.AI

A chat-style web app that generates tailored, professional cover letters from your CV and a job description. Upload PDF, DOCX, or plain text files and get a ready-to-download PDF in seconds.

Built with **FastAPI** (Python) on the backend and **React + Vite** on the frontend, served as a single deployable unit.

---

## What it does

- Upload your CV and/or a job description (PDF, DOCX, or TXT)
- Or paste the job description directly into the chat
- Get a professional cover letter generated by an LLM
- Download it as a formatted PDF with one click
- Chat history is saved locally in a SQLite database

---

## Tech stack

| Layer | Technology |
|---|---|
| Frontend | React 19, Vite 7 |
| Backend | FastAPI, Uvicorn |
| Database | SQLite via SQLAlchemy |
| PDF generation | ReportLab |
| LLM | OpenRouter API (supports 100+ models, including free ones) |
| Container | Docker (single image, multi-stage build) |

---

## Prerequisites

- [Python 3.11+](https://www.python.org/downloads/)
- [Node.js 20+](https://nodejs.org/)
- An [OpenRouter](https://openrouter.ai) API key (free to sign up, free models available)

---

## Local development

### 1. Clone the repo

```bash
git clone https://github.com/your-username/cover-letter-ai.git
cd cover-letter-ai
```

### 2. Set up the backend

```bash
cd backend
python -m venv venv

# On macOS / Linux
source venv/bin/activate

# On Windows
venv\Scripts\activate

pip install -r ../requirements.txt
```

Create a `.env` file in the project root (next to `requirements.txt`):

```bash
cp .env.example .env
```

Open `.env` and paste your OpenRouter API key:

```
OPENROUTER_API_KEY=your_key_here
```

Start the backend server:

```bash
cd backend
uvicorn main:app --reload
```

The API is now running at `http://localhost:8000`.

### 3. Set up the frontend

Open a second terminal:

```bash
cd coverletter-ai
npm install
npm run dev
```

Open `http://localhost:5173` in your browser. The Vite dev server proxies all API calls to the backend automatically, so no extra configuration is needed.

---

## Project structure

```
cover-letter-ai/
├── backend/
│   ├── routers/
│   │   └── coverletter.py   # API routes: /generate, /history, /download
│   ├── db.py                # SQLite models and session management
│   ├── file_utils.py        # Text extraction from PDF, DOCX, TXT
│   ├── llm.py               # OpenRouter API calls
│   ├── main.py              # FastAPI app, serves frontend in production
│   └── pdf_utils.py         # Cover letter → PDF generation
├── coverletter-ai/          # React + Vite frontend
│   └── src/
│       ├── components/
│       │   ├── ChatMessage.jsx
│       │   └── InputBar.jsx
│       ├── api.js
│       ├── App.jsx
│       └── styles.css
├── .env.example
├── .dockerignore
├── Dockerfile
├── requirements.txt
└── README.md
```

---

## Environment variables

| Variable | Required | Default | Description |
|---|---|---|---|
| `OPENROUTER_API_KEY` | Yes | — | Your OpenRouter API key |
| `OPENROUTER_MODEL` | No | `minimax/minimax-m2.5` | Any model slug from openrouter.ai/models |



## Supported file formats

| File type | CV | Job description |
|---|---|---|
| PDF | ✓ | ✓ |
| DOCX | ✓ | ✓ |
| TXT | ✓ | ✓ |
| Pasted text | — | ✓ (via the text box) |

---

## License

MIT